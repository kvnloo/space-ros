---
name: ROS 2 Repos Check

on:
  pull_request:
    branches:
      - "*"

jobs:
  format_and_validity:
    name: Format and Validity
    runs-on: ubuntu-latest
    steps:
      - name: Clone project
        uses: actions/checkout@v7
      - name: Install dependencies
        run: python3 -m pip install vcstool yamllint PyYAML
      - name: Test repository merge script
        run: python3 -m unittest discover -s scripts/tests -p '*_test.py' -v
      - name: Validate formatting
        run: >
          yamllint ros2.repos
          -f github
          -d "{extends: default, rules: {document-start: {present: false}, key-ordering: {}}}"
      - name: Validate repositories
        run: vcs validate --input ros2.repos

  lint:
    name: Space ROS Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: space-ros/space-ros-lint-action@main

  commit_message_standards:
    name: Space ROS Commit Message Standards
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: space-ros/check-commit-message-action@main
